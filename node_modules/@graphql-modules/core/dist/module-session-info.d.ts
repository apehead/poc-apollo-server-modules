import { GraphQLModule } from './graphql-module';
import { ServiceIdentifier } from '@graphql-modules/di';
import { OnRequest } from './types';
export declare class ModuleSessionInfo<Config = any, Session = any, Context = any> {
    private _module;
    private _session;
    constructor(_module: GraphQLModule<Config, Session, Context>, _session: Session);
    readonly module: GraphQLModule<Config, Session, Context>;
    readonly session: Session;
    readonly cache: import("./graphql-module").KeyValueCache;
    readonly context: any;
    readonly injector: import("@graphql-modules/di").Injector;
    readonly config: Config;
    readonly name: string;
    callSessionHook<T extends OnRequest<Config, Session, Context>>(serviceIdentifier: ServiceIdentifier<T>): Promise<void>;
}
