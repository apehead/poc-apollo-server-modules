import { CommunicationBridge } from './communication-bridge';
export declare class EventEmitterCommunicationBridge<EventMap = any> implements CommunicationBridge<EventMap> {
    private readonly _ee;
    subscribe<Event extends keyof EventMap, Payload = EventMap[Event]>(event: Event, handler: (payload: Payload) => void): {
        unsubscribe: () => void;
    };
    publish<Event extends keyof EventMap, Payload = EventMap[Event]>(event: Event, payload: Payload): void;
}
